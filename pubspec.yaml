name: tugas_tabel_meja
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # HTTP Request
  http: ^1.2.1

  # UI Utilities
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.0
  flutter_spinkit: ^5.2.0
  intl: ^0.19.0

  # Responsive Layout (optional)
  responsive_framework: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
